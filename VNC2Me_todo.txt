Todo:

- Use STDIN & STDERR to interact with v2msvr.exe

- Use STDIN, STDOUT & STDERR to interact with v2mvwr.exe

- Setup a chat client

- Create Linux client (using x11vnc? and tightvnc)

- Create MAC Client (not sure of suitable mac VNC Versions)



Done:

- Use STDIN, STDOUT & STDERR to interact with v2mplink.exe
- Create installer package to install on windows.
- Use STDOUT to interact with v2msvr.exe



Can't do:
- Use OpenSSL dll for tunnelling instead of ssh

